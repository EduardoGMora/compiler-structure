#include <stdio.h>

#define LIMIT 50

int counter       

int decrement(int n) {
    n = n - 1;
    return n;
}

int helper(int a, int b) {
    int diff;
    diff = a - b;

    while () {        
        diff = diff - 1;
    }

    return diff + ;   

                      

int main() {
    int x;
    x = 10;

    while (x > 0) {
        x = decrement(x);
    }

    return x;
}
